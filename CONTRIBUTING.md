# Contributing

Use Python 3.11 for local development.

Before opening changes:

1. Install dependencies from `requirements.txt`.
2. Keep generated model files out of Git.
3. Verify the app starts with `streamlit run app.py`.
